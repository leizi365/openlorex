'use client';

import type * as React from 'react';

/** Block actions open from the hover grip icon; right-click menu is disabled. */
export function BlockContextMenu({ children }: { children: React.ReactNode }) {
  return children;
}
